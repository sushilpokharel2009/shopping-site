import { DataobjtoarrPipe } from './dataobjtoarr.pipe';

describe('DataobjtoarrPipe', () => {
  it('create an instance', () => {
    const pipe = new DataobjtoarrPipe();
    expect(pipe).toBeTruthy();
  });
});
